let car: string;
//car = 3; //REMIND 에러가 발생하는건 타입 추론 떄문

export {};

let age: number = 30;
let isAbult: boolean = true;
let a: number[] = [1, 2, 3];
let a2: Array<number> = [1, 2, 3];

let week1: string[] = ['car', 'car2'];

let week2: Array<string> = ['car', 'car2', 'a23fv'];

// week1.push(2); //REMIND 숫자 추가하려면 당연히 에러
